# Pinned to the exact versions used to train house_price.pkl and to
# build/test this backend. scikit-learn MUST match the training version
# exactly, or joblib.load() may warn/behave inconsistently when
# unpickling the fitted Pipeline (ColumnTransformer, OneHotEncoder,
# RandomForestRegressor, TransformedTargetRegressor).
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
pandas==3.0.2
scikit-learn==1.8.0
joblib==1.5.3
numpy==2.4.4
pytest==9.1.1
httpx==0.28.1
python-dotenv==1.2.2
